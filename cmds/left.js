const ytdl = require("ytdl-core")
const queue = new Map()

module.exports.run = async (bot,message,args) =>{
    if(!voice) return message.channel.send("Вы не вошли в голосовой чат, вы меня обидели...")
    else connection.disconnect()

}

module.exports.help ={
    name:"left"
};