const Discord = module.require("discord.js");
const fs = require("fs");
module.exports.run = async (client,message,args) => {
    message.channel.send(`${rUser.user.username}`, "you has banned :D")
}; 
module.exports.help = {
    name: "fake-ban",
	aliases: []
};